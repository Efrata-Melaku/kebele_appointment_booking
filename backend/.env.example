DATABASE_URL="mysql://user:password@localhost:3306/appointment_db"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"
PORT=5000
NODE_ENV=development
FRONTEND_URL="http://localhost:5173"

# Cloudinary (required for file uploads)
# Use the cloud name from https://console.cloudinary.com (e.g. drkrjwk2w).
# Do NOT use "Root" — that is often confused with the MySQL user in DATABASE_URL.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=kebele/documents

MAX_FILE_SIZE=5242880

# Email (Gmail SMTP for development — use App Password)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_FROM=Kebele Appointment System <your_email@gmail.com>

# Legacy local files are served at /uploads automatically (see backend/uploads/)
